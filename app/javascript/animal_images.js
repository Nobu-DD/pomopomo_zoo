document.addEventListener("turbo:load", function () {
  // 動物画像を出現させるオーバーレイdivを取得
  const arena = document.querySelector("#animalSpace");
  // 動物画像のファイルパスを格納しておく配列を作成(画像データは入ってない)
  // const animalImages = 
});